package templates

// MainDoPanelsGo is ./panels.go.
const MainDoPanelsGo = `{{$Dot := .}}package main

import ({{range .Imports}}
	"{{.}}"{{end}}
)

/*

	DO NOT EDIT THIS FILE.

	This file is generated by kickasm and regenerated by rekickasm.

*/

func doPanels(quitCh chan struct{}, tools *viewtools.Tools, callMap map[types.CallID]caller.Renderer, notJS *notjs.NotJS, helper panelHelper.Helper) {
	// 1. Construct the panel code.{{range $name, $path := .PanelNamePathMap}}
	{{call $Dot.LowerCamelCase $name}} := {{$name}}.NewPanel(quitCh, tools, notJS, callMap, helper){{end}}

	// 2. Size the app.
	tools.SizeApp()

	// 3. Start each panel's initial calls.{{range $name, $path := .PanelNamePathMap}}
	{{call $Dot.LowerCamelCase $name}}.InitialCalls(){{end}}
}
`
